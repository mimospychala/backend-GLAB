import styles from "./ProductCard.module.css"
export default function ProductCard(){

    return(
        <div>

    <h2>{product.name}</h2>
    <img src="" alt="" />
    <p>{product.price}</p>

    </div>

    )
}